- hosts: localhost
  vars_files:
    - host_vars/mrc.yaml
    - host_vars/deploy-scale.yaml
  gather_facts: true

  roles:
    - role: openstack-common
    - role: remove-instance
    - role: remove-volume
